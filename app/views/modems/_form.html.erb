<%= form_for(modem) do |f| %>
  <% if modem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(modem.errors.count, "error") %> prohibited this modem from being saved:</h2>

      <ul>
      <% modem.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre_modem %>
    <%= f.text_field :nombre_modem %>
  </div>

  <div class="field">
    <%= f.label :ps_modem %>
    <%= f.text_field :ps_modem %>
  </div>

  <div class="field">
    <%= f.label :oc_modem %>
    <%= f.text_field :oc_modem %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
