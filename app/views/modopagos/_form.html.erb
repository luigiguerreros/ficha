<%= form_for(modopago) do |f| %>
  <% if modopago.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(modopago.errors.count, "error") %> prohibited this modopago from being saved:</h2>

      <ul>
      <% modopago.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :modo_pago %>
    <%= f.text_field :modo_pago %>
  </div>

  <div class="field">
    <%= f.label :descripcion %>
    <%= f.text_field :descripcion %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
