<%= form_for(caracteristicatelefono) do |f| %>
  <% if caracteristicatelefono.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(caracteristicatelefono.errors.count, "error") %> prohibited this caracteristicatelefono from being saved:</h2>

      <ul>
      <% caracteristicatelefono.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pack %>
    <%= f.text_field :pack %>
  </div>

  <div class="field">
    <%= f.label :tipo_pago %>
    <%= f.text_field :tipo_pago %>
  </div>

  <div class="field">
    <%= f.label :costo %>
    <%= f.text_field :costo %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
