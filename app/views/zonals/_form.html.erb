<%= form_for(zonal) do |f| %>
  <% if zonal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(zonal.errors.count, "error") %> prohibited this zonal from being saved:</h2>

      <ul>
      <% zonal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre_zona %>
    <%= f.text_field :nombre_zona %>
  </div>

  <div class="field">
    <%= f.label :descripcion %>
    <%= f.text_field :descripcion %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
