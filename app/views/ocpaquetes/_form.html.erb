<%= form_for(ocpaquete) do |f| %>
  <% if ocpaquete.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ocpaquete.errors.count, "error") %> prohibited this ocpaquete from being saved:</h2>

      <ul>
      <% ocpaquete.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tipo_ocpaquete %>
    <%= f.text_field :tipo_ocpaquete %>
  </div>

  <div class="field">
    <%= f.label :descripcion %>
    <%= f.text_field :descripcion %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
